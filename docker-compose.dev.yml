services:
  db:
    image: postgres:16-alpine
    container_name: jaafar-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jaafar_accounting
    ports:
      - "5432:5432"
    volumes:
      - jaafar_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d jaafar_accounting"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  jaafar_pgdata:
